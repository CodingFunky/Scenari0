// Baked-in defaults for sharing the app publicly.
// The proxy URL is pre-filled so visitors get a working Sync/Odds/Sim
// experience out of the box. Users can still override it via the ⚙ Sync
// settings field (saved to localStorage as 'sync_proxy_url').
export const DEFAULT_PROXY_URL = 'https://worldcup-proxy.worldcupscen.workers.dev';
